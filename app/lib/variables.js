Balanced.COOKIE = {
    MARKETPLACE_URI: 'mru',

    set: function (name, value, options) {
        $.cookie(name, value, options);
    }
};

Balanced.TIME = {
    THREE_YEARS: 365 * 3
};

Balanced.KEYS = {
    ESCAPE: 27
};
