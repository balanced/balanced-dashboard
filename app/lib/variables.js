Balanced.COOKIE = {
    MARKETPLACE_URI: 'marketplaceUri',

    set: function (name, value) {
        $.cookie(name, value);
    }
};

Balanced.TIME = {
    THREE_YEARS: 1095
};

Balanced.KEYS = {
    ESCAPE: 27
};