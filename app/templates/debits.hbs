{{#view "page-navigations/transaction-page-navigation" model=model}}
	<div class="buttons page-actions">
		{{#if model.can_refund}}
			<a class="btn" {{action "openModal" Balanced.Modals.RefundDebitModalView model}}>Refund</a>
		{{/if}}
	</div>
{{/view}}

{{#view "detail-views/body-panel"}}
	{{#view "detail-views/api-model-panel" model=model}}
		{{view Balanced.DebitSummarySectionView model=model}}
		{{view "detail-views/description-lists/transaction-titled-key-values-section" model=model}}
		{{view "meta-list" type=model}}
	{{/view}}

	{{#view "detail-views/main-panel"}}
		{{#if model.dispute}}
			<h3>Associated dispute<i class="icon-disputes"></i></h3>
			<div class="results">
				{{view "results/embedded-disputes-table" loader=disputesResultsLoader}}
			</div>
		{{/if}}

		<h3>Logs</h3>
		<div class="results">
			{{view "resource-logs" content=model}}
		</div>

		<h3>Events</h3>
		<div class="results">
			{{view "resource-events" content=model}}
		</div>
	{{/view}}
{{/view}}
