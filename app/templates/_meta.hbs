<div class="key-value-display">
	<header>
		<h3>Meta</h3>
	</header>
	{{#if view.meta_array}}
	<dl class="dl-horizontal meta">
		{{#each meta in view.meta_array}}
			<dt {{bind-attr title="meta.key"}}>{{meta.key}}</dt>
			<dd><span>{{meta.value}}</span></dd>
		{{/each}}
	</dl>
	{{else}}
		<span class="sl-none">none</span>
	{{/if}}
</div>
