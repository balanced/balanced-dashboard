<h1 class="page-title ellipsis">{{model.message.request.method}} {{model.condensed_request_url}}</h1>

<div class="key-value-display mb-large">
	<dl class="dl-horizontal">
		<dt>Date</dt>
		<dd><time {{bind-attr datetime="model.created_at"}} {{bind-attr title="model.created_at"}}>{{humanReadableDateLong model.created_at}}</time></dd>
		<dt>Method</dt>
		<dd>{{model.message.request.method}}</dd>
		<dt>URI</dt>
		<dd>{{model.short_url}}</dd>
		<dt>Status</dt>
		<dd>{{colorizeStatus model.message.response.status}}</dd>
		<dt>Request ID</dt>
		<dd data-property="request-id">{{model.guru_id}}</dd>
		<dt>IP address</dt>
		<dd>{{model.message.request.headers.X-Real-Ip}} {{model.geo_ip}}</dd>
		<dt>User-agent</dt>
		<dd>{{model.message.request.headers.User-Agent}}</dd>
	</dl>
</div>

<section class="request-info">
	<header>
		<h3>Request body</h3>
	</header>

	{{view Balanced.PrettyPrintView contentBinding="model.message.request.payload"}}
</section>

<section class="mb-none response-info">
	<header>
		<h3>Response body</h3>
	</header>

	{{view Balanced.PrettyPrintView contentBinding="model.message.response.body"}}
</section>
