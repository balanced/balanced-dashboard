<div class="modal hide delete-key">
    <form {{action confirm on="submit"}}>
    <div class="modal-header">
        <button type="button" class="close" {{action hide}}>×</button>
        <h2>Delete this key?</h2>
    </div>
    <div class="modal-body">
        {{partial "modals/modalErrors"}}
        <span class="label7a">Are you sure you want to delete this key?</span>
        {{#if moreWarning}}<br><span class="label7a status-error">CAUTION! This is the current key. You probably don't want to delete this.</span>{{/if}}
        <li>
            <ul>ID: {{key.id}}</ul>
            <ul>Name: {{key.meta.name}}</ul>
            <ul>Created At: {{humanReadableDateShort key.created_at}}</ul>
        </li>
    </div>
    <div class="modal-footer">
        <button class="btn danger" type="button" {{action hide}}>Cancel</button>
        <button class="btn" type="button" {{action confirm}}>Yes</button>
    </div>
    </form>
</div>
<a href="#" {{action "open"}} class="confirm-delete-key">X</a>