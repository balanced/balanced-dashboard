<div class="hold">
	<div {{bind-attr class=":transaction-type-subheader hold.status"}}>
		<div class="tt-label">Hold</div>
		<div class="tt-content">
			<span class="tt-title">
				{{titleCase hold.status}}: {{formatCurrency hold.amount}}
			</span>
			<div class="tt-buttons">
				{{#if hold.can_void_or_capture}}
					{{capture-hold-modal hold=hold}}
					{{void-hold-modal hold=hold}}
				{{/if}}
			</div>
		</div>
	</div>
	<div class="transaction-body">
		<div class="row-fluid">
			<div class="span25 border-adjust">
				<section class="transaction-info">
					<div class="key-value-display">
						<header>
							<h3>Authorization
								{{edit-transaction-modal transaction=hold}}
							</h3>
						</header>
						{{txn-description transaction=hold}}
					</div>
				</section>

				{{txn-funding-instrument funding_instrument=hold.source}}
			</div>
		</div>
		<div class="row-fluid">
			<div class="span25 border-adjust">
				{{view Balanced.ResourceLogsView contentBinding="hold"}}
			</div>
		</div>
		<div class="row-fluid">
			<div class="span25 border-adjust">
				{{view Balanced.ResourceEventsView contentBinding="hold"}}
			</div>
		</div>
	</div>
</div>
