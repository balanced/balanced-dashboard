<section id="main" class="container marketplaces">
	{{#if isApplyPage}}
		{{outlet}}
	{{else}}
		{{view Balanced.MarketplaceSidebarView marketplace=controllers.marketplace.marketplace}}
		<div id="content">
			{{render "temporary_alerts"}}
			{{outlet}}
		</div>
	{{/if}}
</section>
