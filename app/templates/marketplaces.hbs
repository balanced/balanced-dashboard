<section id="main" class="container marketplaces">
	{{#if isApplyPage}}
		{{outlet}}
	{{else}}
		{{partial "marketplace_sidebar"}}
		{{partial "header"}}
		<div id="content">
			{{partial "alert"}}
			{{outlet}}
		</div>
	{{/if}}
</section>
