machine:
  node:
    version: 0.10.22
  dependencies:
    cache_directories:
      - ".bower-cache"    # relative to the build directory
      - ".bower-registry"    # relative to the build directory
      - "node_modules"    # relative to the build directory